name: Update projections

# Daily full refresh (10:00 UTC — after the last games of the previous day in
# North America have finished): re-fetches the fixture list and kickoff times,
# re-runs the Monte-Carlo projections, and guarantees a daily odds-history
# point even on days without games. Prompt post-game updates are handled by
# the update-scores workflow, which rebuilds projections whenever a new
# result comes in.

on:
  schedule:
    - cron: "0 10 * * *" # daily at 10:00 UTC
  workflow_dispatch: {} # lets you trigger a run manually from the Actions tab

permissions:
  contents: write

# Shares a queue with the update-scores workflow so the two never push at once.
concurrency:
  group: update-scores
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: "24"

      - name: Fetch latest results
        env:
          SPORTSDB_KEY: ${{ secrets.SPORTSDB_KEY }}
          WC_SEASON: "2026"
        run: node scripts/fetch_scores.js

      - name: Rebuild projections
        run: node scripts/build_projections.js 20000

      - name: Commit changes if any
        run: |
          git config user.name "scores-bot"
          git config user.email "actions@github.com"
          if [[ -n "$(git status --porcelain data/matches.js data/projections.js data/odds_history.js)" ]]; then
            git add data/matches.js data/projections.js data/odds_history.js
            git commit -m "Update projections ($(date -u +'%Y-%m-%d %H:%M UTC'))"
            git pull --rebase
            git push
          else
            echo "No changes."
          fi
